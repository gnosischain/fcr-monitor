apiVersion: 1

datasources:
  - name: Prometheus
    # Fixed uid so the provisioned dashboard's datasource references resolve.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 12s
